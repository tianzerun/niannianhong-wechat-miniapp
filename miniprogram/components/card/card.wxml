<!--card/card.wxml-->
<view class="card" bindtap="onTapCard">
  <image class="bg-image" src="{{product.imageUrl}}" mode="aspectFill"></image>
  <text class="top-left tag">{{tag}}</text>
  <view class="card-title-wrapper">
    <text class="title">{{product.name}}</text>
  </view>
</view>
